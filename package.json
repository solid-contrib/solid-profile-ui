{
  "name": "solid-profile-ui",
  "version": "0.1.0",
  "description": "Application to view and manage a user's profile",
  "main": "assets/scripts/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack --progress --colors --optimize-minimize --optimize-occurrence-order --optimize-dedupe",
    "build-watch": "webpack --progress --colors --optimize-minimize --optimize-occurrence-order --optimize-dedupe --watch",
    "lint": "standard --verbose"
  },
  "author": "Daniel Friedman",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-core": "^6.9.1",
    "babel-loader": "^6.2.4",
    "babel-preset-es2015": "^6.9.0",
    "handlebars": "^4.0.5",
    "handlebars-loader": "^1.3.0",
    "json-loader": "^0.5.4",
    "standard": "^7.1.2",
    "style-loader": "^0.13.1",
    "webpack": "^1.13.1"
  },
  "dependencies": {
    "minivents": "^2.0.2",
    "rdflib": "^0.7.1",
    "solid-client": "^0.20.0",
    "spectre.css": "^0.1.24",
    "whatwg-fetch": "^1.0.0"
  }
}
